<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_gravity="fill_horizontal">

    <TextView
        android:id="@+id/widget_ball_text"
        style="@style/Widget.Text.NumberBall"
        android:layout_width="@dimen/number_ball_widget_size"
        android:layout_height="@dimen/number_ball_widget_size"
        android:layout_gravity="center"
        android:layout_margin="@dimen/extra_small_padding"
        android:background="@drawable/widget_ball_background"
        android:gravity="center"
        android:text="@string/widget_ball_placeholder_text"/>

</FrameLayout>